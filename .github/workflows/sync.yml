name: sync

on:
    schedule:
        - cron: "0 0 * * *"
    workflow_dispatch:

jobs:
    build:
        runs-on: macos-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4
            - name: Run Main
              run: |
                    sudo chmod +x sync.sh
                    ./sync.sh
